# RunPod Serverless Configuration (SERVER-SIDE ONLY - do NOT use VITE_ prefix)
# Get endpoint ID from: https://www.runpod.io/console/serverless
# Get API key from: https://www.runpod.io/console/user/settings

# Builder Serverless Endpoint (handles ingest + embedding + assembly)
# Format: https://api.runpod.ai/v2/{YOUR_ENDPOINT_ID}/run
BUILDER_API_URL=https://api.runpod.ai/v2/YOUR_BUILDER_ENDPOINT_ID/run
RUNPOD_API_KEY=your_runpod_api_key_here

# For local development, you can use localhost if running the builder locally
# BUILDER_API_URL=http://localhost:8080
# RUNPOD_API_KEY=not_needed_for_local

# R2 (Cloudflare) Storage Configuration
# docpacks go in doctown-central/docpacks/[repo_owner]/[repo_name].docpack on output
BUCKET_NAME=your-bucket-name
BUCKET_ACCESS_KEY_ID=your_access_key_id
BUCKET_SECRET_ACCESS_KEY=your_secret_access_key
BUCKET_S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
BUCKET_PUBLIC_URL=https://pub-xxx.r2.dev  # Get this from R2 bucket settings after enabling public access
